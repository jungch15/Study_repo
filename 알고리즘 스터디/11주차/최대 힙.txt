import heapq
import sys

h = []
n = int(sys.stdin.readline())
for i in range(n):
    c = int(sys.stdin.readline())
    if c == 0:
        if len(h) == 0:
            print(0)
        else:
            print(heapq.heappop(h) * -1)
    else:
      heapq.heappush(h,-c)